import 'package:flutter/material.dart';
import 'package:flutter_icons/flutter_icons.dart';

class CustomIcon {
  static const Icon slider = Icon(FontAwesome.sliders);
  static const Icon bag = Icon(FontAwesome.shopping_bag, color: Colors.black);
  static const Icon search = Icon(
    FontAwesome.search,
    size: 20,
    color: Colors.grey,
  );
  static const Icon home = Icon(
    FontAwesome.home,
    color: Colors.black,
  );
  static const Icon user = Icon(
    FontAwesome.user,
    color: Colors.black,
  );
  static const Icon heart = Icon(
    FontAwesome.heart,
    color: Colors.black,
  );
  static const Icon shop = Icon(
    FontAwesome.shopping_cart,
    color: Colors.black,
  );
  static const Icon camera = Icon(
    FontAwesome.camera,
    color: Colors.white,
  );
  static const Icon pencil = Icon(
    FontAwesome.pencil,
    color: Colors.white,
  );
  static const Icon back = Icon(
    FontAwesome.arrow_left,
    color: Colors.black,
  );
  static const Icon backW = Icon(
    FontAwesome.arrow_left,
    color: Colors.white,
  );
  static const Icon right = Icon(
    FontAwesome.arrow_circle_o_right,
    color: Colors.black,
  );

  static const Icon outlineHeart = Icon(
    FontAwesome.heart_o,
    color: Colors.black,
    size: 25,
  );

  static const Icon redHeart = Icon(
    FontAwesome.heart,
    color: Colors.red,
  );

  static const Icon remove = Icon(
    FontAwesome.times_circle_o,
    color: Colors.red,
    size: 70,
  );
  static const Icon profileUser = Icon(FontAwesome.user_circle);

  static const Icon cart = Icon(
    FontAwesome.shopping_cart,
    size: 30,
  );

  static const Icon check = Icon(
    FontAwesome.check_circle_o,
    size: 70,
    color: Colors.green,
  );
}
